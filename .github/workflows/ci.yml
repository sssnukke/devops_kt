name: CI - Tests and Docker build

on:
  push:
    branches:
      - main
      - master

jobs:
  test:
    name: Run tests (py ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.9', '3.10', '3.11']  # три версии Python
      fail-fast: false

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}

      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      - name: Run tests (pytest)
        # Предполагаем, что проект использует pytest; подставь команду, если другая
        run: |
          pip install pytest || true
          pytest --maxfail=1 -q

  build-docker:
    name: Build Docker image (only if tests passed)
    runs-on: ubuntu-latest
    needs: test
    # Запустится только если все matrix задач из job "test" завершились успешно.
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image (no push)
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: false
          tags: my-app:ci-build  # локальный тег внутри раннера; поменяй при необходимости
